#pragma once

#include <nlohmann/json.hpp>
#include <string>

namespace reactdom {

constexpr auto import_css = [](const std::string& path) {
    // document write css
    // download json and return
};

} // namespace reactdom
